---
id: jobs-match-all
title: Analisar Compatibilidade com Vagas
---

## Descricao

Analisa a compatibilidade do usuario autenticado com todas as vagas disponiveis no banco de dados usando IA (OpenAI GPT). Para cada vaga, o GPT gera um score de 0 a 100, uma justificativa e listas de pontos fortes e gaps do candidato.

O fluxo funciona em tres etapas:
1. **Pre-filtragem**: seleciona ate 15 vagas com maior correspondencia de skills com o perfil do usuario.
2. **Analise por IA**: o GPT avalia a compatibilidade individualmente para cada vaga selecionada.
3. **Persistencia**: os resultados sao salvos no banco e retornados ordenados por score (maior primeiro).

:::info Pre-requisito
O usuario deve ter vagas disponiveis no banco antes de executar este endpoint. Utilize `GET /api/jobs/search` para buscar e salvar vagas primeiro.
:::

:::caution Custo de processamento
Este endpoint faz uma chamada a API da OpenAI para cada vaga analisada (ate 15). Utilize com moderacao para evitar custos excessivos.
:::

## Autenticacao

- Necessita autenticacao: Sim
- Tipo: Bearer JWT
- Header: `Authorization: Bearer <access_token>`

## Request

### Metodo e URL

`POST /api/jobs/match-all`

### Headers

| Header        | Valor                   |
|---------------|-------------------------|
| Authorization | `Bearer <access_token>` |

### Query Params

Nenhum.

### Body

Nenhum.

## Response

### Sucesso

Status: `200 OK`

Retorna a lista de matches ordenada por score decrescente (melhor compatibilidade primeiro).

```json
[
  {
    "jobId": 1,
    "titulo": "Backend Developer - Java",
    "empresa": "Tech Corp",
    "localizacao": "Sao Paulo, SP",
    "modeloTrabalho": "Remote",
    "jobUrl": "https://www.linkedin.com/jobs/view/abc123",
    "score": 87.5,
    "justificativa": "O candidato possui experiencia solida em Java e Spring Boot, que sao os principais requisitos da vaga. As skills de PostgreSQL e Docker tambem estao alinhadas com o stack da empresa.",
    "pontosFortres": ["Java", "Spring Boot", "PostgreSQL"],
    "gaps": ["Kubernetes", "AWS"]
  },
  {
    "jobId": 2,
    "titulo": "Java Software Engineer",
    "empresa": "Startup X",
    "localizacao": "Remoto",
    "modeloTrabalho": "Remote",
    "jobUrl": "https://www.indeed.com/viewjob?jk=def456",
    "score": 62.0,
    "justificativa": "O candidato atende os requisitos basicos de Java, mas a vaga exige experiencia com microservicos e mensageria que nao estao presentes no perfil.",
    "pontosFortres": ["Java", "REST APIs"],
    "gaps": ["Kafka", "Microservices", "Kubernetes"]
  }
]
```

### Campos da Resposta

| Campo          | Tipo         | Descricao                                                        |
|----------------|--------------|------------------------------------------------------------------|
| jobId          | Long         | ID interno da vaga no banco de dados                             |
| titulo         | String       | Titulo da vaga                                                   |
| empresa        | String       | Nome da empresa                                                  |
| localizacao    | String       | Localizacao da vaga                                              |
| modeloTrabalho | String       | Modelo de trabalho (Remote, Hybrid, On-site)                     |
| jobUrl         | String       | Link para a vaga original                                        |
| score          | Double       | Pontuacao de compatibilidade de 0 a 100                          |
| justificativa  | String       | Explicacao do score em 2-3 frases gerada pela IA                 |
| pontosFortres  | Array String | Skills e experiencias do candidato alinhadas com a vaga          |
| gaps           | Array String | Requisitos da vaga que o candidato nao possui                    |

### Criterios de Score

| Faixa     | Significado                                              |
|-----------|----------------------------------------------------------|
| 80 – 100  | Candidato atende a maioria dos requisitos                |
| 60 – 79   | Candidato atende requisitos basicos com alguns gaps      |
| 40 – 59   | Candidato tem base mas falta experiencia relevante       |
| 0 – 39    | Pouca compatibilidade                                    |

### Erros

| Status | Descricao |
|--------|-----------|
| `400 Bad Request` | Nenhuma vaga encontrada no banco. Execute `GET /api/jobs/search` primeiro. |
| `401 Unauthorized` | Token JWT ausente ou invalido. |

Exemplo de erro `400`:

```json
{
  "message": "Nenhuma vaga encontrada. Faca uma busca de vagas primeiro."
}
```

## Regras de Negocio

- O perfil do candidato enviado a IA inclui: cargo desejado, resumo profissional, skills, experiencias e projetos.
- Se o usuario nao tiver skills cadastradas, as primeiras 15 vagas sao selecionadas sem filtragem.
- Se houver skills, as vagas sao pre-filtradas por correspondencia textual com titulo, descricao e requisitos, priorizando as com mais matches.
- Matches anteriores da mesma vaga para o mesmo usuario sao deletados antes de salvar o novo resultado.
- Resultados sao retornados ordenados por score (maior para menor).

## Fluxo Resumido

1. Extrai o `userId` do JWT no header `Authorization`.
2. Busca todas as vagas no banco — lanca `400` se nao houver nenhuma.
3. Busca as skills do usuario.
4. Pre-filtra ate 15 vagas com maior correspondencia de skills (ou as 15 primeiras, se sem skills).
5. Monta o texto de perfil do usuario (cargo, resumo, skills, experiencias, projetos).
6. Para cada vaga filtrada:
   a. Deleta o match anterior (se existir).
   b. Envia perfil + vaga ao GPT e recebe score, justificativa, pontosFortres e gaps.
   c. Salva o resultado como `JobMatch` no banco.
7. Retorna a lista de matches ordenada por score com `200 OK`.