---
id: intro
title: Search Jobs API
sidebar_position: 1
---

## Descricao

Esta documentacao descreve a API `Search Jobs`, implementada em Spring Boot 3.5 com arquitetura hexagonal (`application`, `domain`, `infrastructure`) e autenticacao baseada em JWT Bearer.

## Visao Geral

- Base path da API: `/api`
- Banco de dados: PostgreSQL
- Formato de autenticacao: `Authorization: Bearer <access_token>`
- Upload de arquivos: multipart/form-data, somente PDF, maximo 10 MB
- Extracao de dados: OpenAI GPT

## Rotas Disponiveis

### Autenticacao (publicas)

| Metodo | Rota                   | Descricao              |
|--------|------------------------|------------------------|
| POST   | `/api/auth/register`   | Registrar novo usuario |
| POST   | `/api/auth/login`      | Autenticar usuario     |

### Curriculos (requerem autenticacao)

| Metodo | Rota                        | Descricao                             |
|--------|-----------------------------|---------------------------------------|
| POST   | `/api/resumes/upload`       | Enviar curriculo em PDF               |
| GET    | `/api/resumes`              | Listar curriculos do usuario logado   |
| POST   | `/api/resumes/{id}/extract` | Extrair dados do curriculo via IA     |
| DELETE | `/api/resumes/{id}`         | Deletar curriculo                     |

### Vagas (requerem autenticacao)

| Metodo | Rota                  | Descricao                                              |
|--------|-----------------------|--------------------------------------------------------|
| GET    | `/api/jobs/search`    | Buscar vagas via JSearch + IA ordenadas por relevancia |
| POST   | `/api/jobs/match-all` | Calcular compatibilidade com todas as vagas via IA     |
| GET    | `/api/jobs/matches`   | Retornar matches previamente calculados                |

### Candidaturas (requerem autenticacao)

| Metodo | Rota                            | Descricao                            |
|--------|---------------------------------|--------------------------------------|
| GET    | `/api/applications`             | Listar candidaturas em formato Kanban|
| POST   | `/api/applications`             | Criar nova candidatura               |
| PUT    | `/api/applications/{id}/status` | Atualizar status da candidatura      |
| DELETE | `/api/applications/{id}`        | Deletar candidatura                  |

### Perfil (requerem autenticacao)

| Metodo | Rota                          | Descricao                                    |
|--------|-------------------------------|----------------------------------------------|
| GET    | `/api/profile`                | Retornar perfil do usuario                   |
| PUT    | `/api/profile`                | Atualizar perfil do usuario                  |
| GET    | `/api/profile/skills`         | Listar skills                                |
| PUT    | `/api/profile/skills`         | Substituir todas as skills                   |
| GET    | `/api/profile/experiences`    | Listar experiencias profissionais            |
| PUT    | `/api/profile/experiences`    | Substituir todas as experiencias             |
| GET    | `/api/profile/certifications` | Listar certificacoes                         |
| PUT    | `/api/profile/certifications` | Substituir todas as certificacoes            |
| GET    | `/api/profile/projects`       | Listar projetos                              |
| PUT    | `/api/profile/projects`       | Substituir todos os projetos                 |
| POST   | `/api/profile/github/sync`    | Sincronizar projetos com repositorios GitHub |

### Configuracoes (requerem autenticacao)

| Metodo | Rota            | Descricao                                   |
|--------|-----------------|---------------------------------------------|
| GET    | `/api/settings` | Retornar configuracoes do usuario           |
| PUT    | `/api/settings` | Substituir todas as configuracoes           |
| PATCH  | `/api/settings` | Atualizar parcialmente as configuracoes     |

### Notificacoes (requerem autenticacao)

| Metodo | Rota                            | Descricao                          |
|--------|---------------------------------|------------------------------------|
| GET    | `/api/notifications`            | Listar notificacoes do usuario     |
| PATCH  | `/api/notifications/{id}/read`  | Marcar notificacao como lida       |

## Regras Tecnicas Relevantes

- Rotas em `/api/auth/**` sao publicas.
- Todas as demais rotas exigem token JWT valido no header `Authorization`.
- O access token expira em `900000 ms` (15 minutos).
- O refresh token expira em `604800000 ms` (7 dias).
- Erros de validacao retornam `422 Unprocessable Entity` com mapa de campos invalidos.
- O padrao de resposta de erro e `ErrorResponse` com os campos: `status`, `erro`, `mensagem`, `path`, `timestamp` e `campos` (somente em erros de validacao).

## Referencias Internas

| Modulo         | Controller                   | Servico                                                |
|----------------|------------------------------|--------------------------------------------------------|
| Autenticacao   | `AuthController`             | `AuthService`                                          |
| Curriculos     | `ResumeController`           | `ResumeService`, `ResumeExtractionService`             |
| Vagas          | `JobController`              | `JobSearchService`, `JobMatchService`                  |
| Candidaturas   | `ApplicationController`      | `ApplicationService`                                   |
| Perfil         | `UserProfileController`      | `UserProfileService`                                   |
| Configuracoes  | `UserSettingsController`     | `UserSettingsService`                                  |
| Notificacoes   | `NotificationController`     | `NotificationService`                                  |

## Swagger UI

A documentacao interativa esta disponivel em `http://localhost:8080/swagger-ui.html` com todos os endpoints, schemas e exemplos de request/response.